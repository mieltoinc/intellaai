## Agent adapters

- **Claude Code** (`claude`) — Claude via the `claude` CLI and Claude Agent SDK.
- **Codex** (`codex`) — OpenAI Codex via the Codex CLI.
- **OpenCode** (`opencode`) — OpenCode SDK adapter.
